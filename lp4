import pandas as pd

# Загрузка данных из файла(файл в директории с проектом)
data = pd.read_csv('Customers.csv', delimiter=';')

# Проверим наш датафрейм на колл-во пропущенных значений. В столбце Профессия 35 шт
print(data.isnull().sum())

# Применим метод, заменим пустые значения на строку "Пустое значение", атрибут inplace изменит таблицу на месте
data.fillna(value='Пустое значение', inplace=True)

# Тут пропущенных значений уже нет
print(data.isnull().sum())

# Вводим новую переменную, чтобы не изменять нашу основную таблицу, группируем данные по столбцу Профессия и вычисляем
# среднее значение в столбце Income при помощи функции mean
mean_data = data.groupby('Profession')['Income'].mean()

print(mean_data)
